# Ignore the node_modules directory, which contains third-party libraries and dependencies
node_modules

# Ignore the migrations directory, which typically contains database migration scripts
migrations

# Ignore database journal files used during development
dev.db-journal

# Ignore the development database file
dev.db

# Ignore environment configuration files, which may contain sensitive data
.env

# Ignore the uploads directory, where user-generated content is stored
uploads